import { LayoutProps } from "@/components/layout";

function AdminLayout({
  mode = "admin",
  menu = "home",
  currentPage,
  children,
}: LayoutProps) {
  return <div></div>;
}

export default AdminLayout;
